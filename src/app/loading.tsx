const Loading = () => {
  return (
    <div className="flex h-screen items-center justify-center bg-black">
      <div className="size-10 animate-spin rounded-full border-4 border-gray-800 border-t-white" />
    </div>
  );
};

export default Loading;
